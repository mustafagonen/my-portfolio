import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [

  // { path: 'dashboard', title: "Anasayfa", component: DashboardComponent },

  { path: '', title: "MG", component: AppComponent },
];
